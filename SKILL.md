---
name: goal-loop-light
description: 帮你在执行前把模糊任务想清楚。把一个"帮我做XX"改写成有目标、有成功标准、有阻塞扫描、有推进策略的结构化计划。适用于执行前需要厘清方向、避免做着做着跑偏的场景。
---

# goal-loop-light

把一句话的模糊委托，变成一个结构化、可执行的任务规划。

```
用户说"帮我做一个竞品调研"
→ goal-loop-light 给出：目标是什么，什么算完成，不能牺牲什么，
  整个闭环需要什么资源，每一圈产出什么，什么时候必须问人
```

## 什么时候用

```text
任务不是一轮回答能完成
用户说的是"继续推进 / 优化一下 / 做得更好"
过程中可能需要反馈、修正或验收
你自己也担心做着做着会偏
```

## 结构化推导：8 步

触发后，按以下顺序逐步推导。不要跳步。每一步都有特定的任务和产出。

### 第 1 步：终态描述 → 目标

问自己：完成后世界长什么样？能否用一句话让不了解背景的人知道你要做什么？

```text
❌ "优化一下代码" / "继续推进" / "让它更好"
   → 没有终态，执行中会自动漂移
✅ "让 CLI 支持图片和文字一起进入 agent 上下文，并通过一次真实验证"
   → 有终态，可以判断是否到达
```

### 第 2 步：成功标准（分层）

成功标准必须分两层：

- **原型层**：系统是否工作？数据能通、验证可通过。这一层在执行中就能验证。
- **效果层**：目标是否真正达成？用户行为会有什么可观察的变化？如果效果层在执行时无法直接验证，必须设计替代信号——用已知正例/反例、抽样验证、模拟数据。

判断标准：能否让一个不了解过程的人，仅凭证据判断"完成了"？

### 第 3 步：约束

约束写的是"不能牺牲什么"，不是"应该怎么做"。

- **硬约束**：违反后不可修复（数据丢失、安全泄露）
- **软约束**：违反后可修复但有成本

约束的思考方法：问自己——如果达成目标的方式让你觉得"不对"，那个"不对"就是约束。

### 第 4 步：启动前阻塞扫描

看整个闭环，不只看当前一圈。凡是迟早需要的资源、凭证、权限、外部服务选择、预算、数据边界、责任判断，都提前列出。

每个阻塞项判断三件事：
1. 能否由人类现在提前解除？
2. 不能解除，是否有 mock / dry-run / 占位接口 / 缩小目标等替代路径？
3. 不能解除也不可替代 → 这才是当前阻塞

运行中才会暴露的问题（接口异常、测试失败）只在执行后处理，不提前猜测。

### 第 5 步：检查安排

不是最后验收，而是每一圈的校准机制。说明：
- 主 agent 自查什么产物和证据
- 独立检查 agent 复查什么缺口和风险
- 什么检查结果会触发人类介入

人类不做默认检查者，只在触发条件满足时介入。

### 第 6 步：持续推进策略

不只是写一个"下一步"，而是排出完整的推进路径：先做什么，做完怎么检查，没过怎么修，过了怎么继续，什么时候必须问人或完成。

每一圈绑定三个判断：
- **目标判断**：这一圈离目标更近了吗？
- **产物判断**：这一圈产出了什么可检查的东西？
- **权限判断**：这个决定我有资格独自做吗？

### 第 7 步：可达性验证

把所有圈串起来，逐一检查：每一圈关闭了最终目标的哪个缺口？所有成功标准都被至少一圈覆盖了吗？如果有标准没被覆盖，补圈或重设计。

### 第 8 步：向用户提问

8 步走完，向用户收口。只问三类：

**需要你校准的判断**：会改变目标、成功标准、约束或责任边界的问题。没有就写暂无。

**可提前解除的阻塞**：用户现在能提供、且能减少后续停顿的资源、凭证、权限或决策。没有就写暂无。

**默认推进策略**：如果用户暂时不确认，如何继续推进可逆、可检查的工作。

---

不要问的三类：
- agent 可以默认推进的问题
- 只需记录、不影响当前推进的偏好
- 可以用 mock / dry-run 继续处理的细节

## 人类必须检查

对于一个模糊任务，agent 擅长的是"结构化"——把一个模糊的委托变成有目标、有标准、有约束的框架。但 agent 不擅长判断"这个框架是否真正匹配你的意图"。

这恰好是你擅长的：你看到 agent 给出的澄清和框架，能判断"这确实是我想要的"还是"不对，方向偏了"。

这不是 agent 做得不够好。是各自做各自适合的事：

```text
agent 负责结构化提案
人类负责判断提案是否对准了模糊背后的真实需求
```

所以 8 步的最后一步是"向用户提问"——不是礼貌，是让人的判断力在关键节点介入。Plan Phase 不经过你确认，不算完成。

## 输出格式

推导完成后，输出以下格式的目标循环：

```markdown
## 目标循环

**目标:** [完成后的世界长什么样]

**成功标准:** [分层：原型层 + 效果层，效果层写用户行为的可观察变化]

**约束:** [不能用什么代价达成，哪些边界不能越过]

**当前状态:** [现在已经有什么，还缺什么]

**启动前阻塞扫描:** [哪些能提前解除、哪些可替代、哪些是当前阻塞]

**检查安排:** [主 agent 自查什么、独立检查什么、什么触发人类介入]

**持续推进策略:** [完整路径，每圈绑定可检查产物和三个判断]

**反馈规则:** [新反馈来了怎么处理]

**人类介入点:** [什么时候必须问人，区分当前阻塞 / 未来阻塞 / 记录型]

**暂停条件:** [什么时候不能继续；暂停时交代状态、证据、卡点和接续点]

**完成条件:** [什么证据证明满足成功标准；完成时必须做完成审计]

## 请你确认 / 提供

**需要你校准的判断:** [只列会改变方向的问题]
**可提前解除的阻塞:** [用户现在能提供的输入]
**默认推进策略:** [如果暂时不确认，怎么继续]
```

## 判断边界

### 需要问人

```text
目标是否变化
成功标准是否变化
多个路线都合理但代价不同
用户体感是否接受
涉及钱、客户、法律、隐私或长期架构
真实凭证、付费资源、外部服务或外发数据需要人确认
```

### 不需要问人

```text
修正明确错字
执行已确认计划
整理当前状态
跑已确认的验证
在可逆范围内调整路径
完成阶段收口后继续下一圈
```

### 人类介入点的判断方法

不是背清单，而是问两个问题：

```text
问题 A：这个决定如果错了，能撤回吗？
  → 能撤回 → agent 可以先做再看
  → 不能撤回 → 停下来问人

问题 B：做这个决定需要的判断，本质上属于谁？
  → 纯技术判断（agent 有完整信息）→ agent 做
  → 涉及用户偏好/产品方向/业务取舍 → 问人
  → 用户表达了主观感受（"感觉不对"）→ 解释权在用户，必须问
```

组合判断：

```text
可逆 + 解释权在 agent → 直接做
可逆 + 解释权在用户 → 做了但标记，下次同步
不可逆 + 解释权在 agent → 做 + 通知
不可逆 + 解释权在用户 → 停下来问
```

## 反馈来了怎么处理

四个问题：

```text
1. 这是什么类型的信号？错误 / 偏好 / 风险 / 机会 / 约束变化 / 目标变化
2. 谁有资格解释这个信号？agent 能自己判断，还是需要用户确认？
3. 这个信号应该更新什么？目标 / 计划 / 标准 / 风险记录 / 仅观察
4. 这个信号对循环的影响是什么？继续 / 重试 / 改计划 / 暂停 / 完成
```

过滤方法：每个信号问——**它改变了我对"离目标多远"的估计吗？**

```text
改变了距离估计 → 立即响应（可能需要改路径）
改变了路径偏好 → 记录，在下一个决策点考虑
不改变任何估计 → 记录但不行动
与目标无关 → 丢弃
```

## 完成审计

不是问"我做完了吗"，而是问"一个不了解过程的人，看到我的证据，会同意这是完成了吗？"

必须回答：

```markdown
## 完成审计

**原目标:** ...
**成功标准:** ...
**交付物与证据:** ...
**遗留风险:** ...
**结论:** [我如何知道它完成了]
```

## 示例

### 示例 1：调研任务

用户说："帮我把这个调研任务推进完。"

```markdown
## 目标循环

**目标:** 完成竞品 AI 助手功能差异调研，形成一份可以用于决策的简明结论，
说明可选方案、主要证据、风险和推荐路径。

**成功标准:**
- （原型层）至少比较 3 个竞品，每个有核心依据和主要风险
- （原型层）输出一个明确推荐，标出哪些结论确定、哪些需要人确认
- （效果层）决策者能在 5 分钟内说清倾向哪个方案及其理由

**约束:**
- 优先使用可靠来源，不把未经验证的信息写成确定结论
- 不展开成过长报告，先给可决策版本

**当前状态:** 已有一个初步问题，但还没有系统比较方案，也没有形成推荐。

**启动前阻塞扫描:**
- 已必需输入：调研问题、目标竞品、决策场景
- 可提前解除：用户给出调研范围和风险偏好
- 可预判未来阻塞：推荐进入客户交付或公开发布前需要确认责任边界
- 缺失默认策略：先用标注不确定性的草案继续推进

**检查安排:**
- 主 agent 自查：每一圈是否产出候选方案表、比较结论或最终结论；
  产物是否有证据、风险和待确认项
- 独立检查 agent 复查：是否证据不足、遗漏关键竞品、把猜测写成结论
- 触发人类介入：推荐涉及风险偏好或高代价取舍时

**持续推进策略:**
- 第一圈：产出候选方案表（至少 3 个竞品，证据 + 风险）
  → 关闭"有哪些选项"缺口
- 第二圈：产出比较结论和推荐路径
  → 关闭"哪个更好"缺口
- 第三圈：产出最终简明结论、风险清单和待确认问题
  → 关闭"结论是什么"缺口
- 可达性验证：三圈覆盖所有成功标准

**反馈规则:** 新资料只补充细节则记录；推翻关键假设则更新推荐；
资料来源不可靠只作为线索，不进入结论。

**人类介入点:**
- 当前阻塞：必须在多个高代价推荐路径中选择一个时
- 未来阻塞：推荐进入客户交付或公开发布前
- 记录型：需要确认决策偏好但不影响证据整理时

**暂停条件:** 关键证据缺失且无法验证时，暂停并说明缺口、已完成证据和接续点。

**完成条件:** 推荐路径、证据、风险和待确认问题都写清楚，完成一次审计后本轮完成。

## 请你确认 / 提供

**需要你校准的判断:** 这次调研会用于真实决策、客户交付还是内部参考？
如果是真实交付，推荐路径的风险偏好需要你确认。

**可提前解除的阻塞:** 如果有指定资料源、必须比较的方案、内部约束或不能采用的路线，可以现在提供。

**默认推进策略:** 暂时不提供的话，先用公开资料和标注不确定性的候选方案表推进。
```

### 示例 2：产品改进任务

用户说："帮我把这个功能体验优化一下。"

```markdown
## 目标循环

**目标:** 找出这个功能当前最影响用户体验的 1-2 个问题，完成一轮低风险改进，
使用户能更顺畅地完成核心动作。

**成功标准:**
- （原型层）能说清当前体验问题是什么；至少完成一处可验证的小改动；
  能通过一次基本使用验证；改进方案不改变功能主目标
- （效果层）用户完成核心动作的时间或步骤数减少，
  或主观反馈中不再提及该卡点

**约束:**
- 不做大范围重构，不引入新的复杂依赖
- 不用主观喜好替代用户体感

**当前状态:** 已知道用户觉得体验不顺，但还没有定位具体卡点。

**启动前阻塞扫描:**
- 已知必需输入：功能目标、核心使用路径、允许改动范围
- 可提前解除：用户确认功能目标、使用路径、改动权限和不可碰的边界
- 可预判未来阻塞：改动影响真实用户或改变产品定位前需要确认发布权限
- 缺失默认策略：先做低风险、可回退的小改或原型记录

**检查安排:**
- 主 agent 自查：每一圈是否产出使用路径记录、卡点、小改、验证结果；
  产物是否仍在目标和约束内
- 独立检查 agent 复查：是否漏掉更明显的卡点；是否把主观喜好当成体验问题
- 触发人类介入：涉及产品方向变化、用户群取舍或核心流程改变时

**持续推进策略:**
- 第一圈：产出使用路径记录和最明显卡点列表
  → 关闭"问题在哪"缺口
- 第二圈：产出一处小改、验证结果和遗留风险
  → 关闭"改了有没有效"缺口
- 验证未通过但原因明确 → 继续修；
  通过且未触发人类介入 → 继续下一个卡点或进入完成审计
- 可达性验证：两圈覆盖原型层标准；效果层用任务完成时间和步骤数变化作为替代信号

**反馈规则:** 明确交互摩擦 → 先做小改；
涉及产品方向或目标用户变化 → 停下来请人判断。

**人类介入点:**
- 当前阻塞：改变核心流程且不可回退；牺牲某类用户体验换另一类且影响产品方向
- 未来阻塞：改动进入正式发布、影响真实用户或改变长期产品定位前
- 记录型：需要重新定义功能目标；当前小改不依赖此判断时先完成本轮

**暂停条件:** 问题来自更上层产品定位、低风险改动无法判断时，
暂停并说明卡点、已验证改动、遗留风险和接续点。

**完成条件:** 明确卡点被修正、通过基本使用验证、遗留风险已记录后本轮完成。

## 请你确认 / 提供

**需要你校准的判断:** 是否存在不可改的核心流程、不能牺牲的用户群体体验，
或这轮不能触碰的产品边界？

**可提前解除的阻塞:** 如果有用户反馈、录屏、日志、失败路径或发布限制，现在可以提供。

**默认推进策略:** 暂时不提供的话，先基于现有路径做低风险复现和可回退小改。
```

---

## 一个局限

当前版本聚焦在执行前的规划——帮你想清楚一个模糊任务"到底要做什么"和"怎么做不会偏"。规划完成后，如何让 agent 自动推进每一圈、在执行中不丢失状态，是正在探索的方向。
