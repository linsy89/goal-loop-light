---
name: goal-loop-light
description: 轻量目标循环：把一个模糊的多轮任务改写成 agent 能持续推进、能检查、能在关键时刻问人的执行闭环。适用于任务模糊、需要多轮推进、容易漂移，或过程中需要反馈和人类介入的场景。
---

# goal-loop-light

## 默认定位

goal-loop-light 不是纯提示语，也不是完整后台 harness。

默认结构是：

```text
goal-loop-light = SKILL.md + Loop State + Session Runner Script
```

这是硬标准，不是可选建议。

```text
没有 Loop State，不算启动 goal-loop-light。
没有 Runner Check，不算完成一轮 goal-loop-light。
Runner 返回 continue，agent 必须继续执行 next_step，不得询问用户是否继续。
只有 Runner 返回 human_gate / pause / complete 时，agent 才能停下来交付状态。
```

三部分职责：

```text
SKILL.md
定义判断原则：目标、成功标准、约束、反馈、人类介入点、暂停条件和完成条件。
告诉模型"怎么思考"，不是"怎么填表"。

Loop State
保存当前任务的结构化状态，是活的文档——每圈开始前根据新证据重新审视和更新。
位置：.goal-loop/loop_state.json

Session Runner Script
读写 Loop State，判断当前状态是 continue / human_gate / pause / complete，并生成下一轮执行要求。
位置：scripts/session_runner.py
```

边界：

```text
它不是后台 harness。
它不负责跨 session 自动恢复。
它不负责多 agent 调度。
它不默认自己调用模型 API。
它不绕过人类介入点、权限边界、成本边界、隐私边界或不可逆操作。
```

## 执行架构：两个 Phase

触发 goal-loop-light 后，**必须按顺序执行两个 Phase，不可跳过或合并**。

```
Phase 0 — Plan & Align（宏观规划，只执行一次）
    ↓  输出对齐后的 Loop State
Phase 1 — Execute Loop（执行循环，每圈重新微观规划）
    ↓  持续运行直到 Runner 返回 human_gate / pause / complete
交付状态
```

### Phase 0 — Plan & Align（触发时只执行一次）

Plan Phase 的目标：**把一个模糊任务改写为结构化的、可执行的闭环**。这不是填表，而是结构化思考。

#### Phase 0 和 Runner 的分工

```text
Phase 0 负责"做对的事" —— 靠推理和对话
Runner 负责"把对的事持续做对" —— 靠 schema 和四选一判断
Phase 0 的产出是 Runner 的输入；Runner 的验证结果是 Phase 0 的结构质量信号。
```

#### 核心原则（骨架）

Phase 0 的 9 步推导围绕三根骨架生长。每一步都在回答：**我在这条线上做了什么？**

```text
方向线 — 目标不漂移：终态描述和成功标准必须让不熟悉背景的人也能判断是否到达。
产物线 — 产物不断供：每一圈必须有可检查的东西，不能停在"定义清楚"。
解释权线 — 不越界：agent 不代替人类做价值判断，只在有完整信息的技术判断上自主。
```

#### Phase 0 内部的张力

**张力 T1：定义清楚 vs 急着动手**

```text
永远在定义，永远不动手 ←————————— 平衡点 ———————————→ 定义都没对齐就开干

平衡点：9 步走完、对齐完成就进入 Phase 1。不要追求"完美的定义"，
要追求"足够开始的最小完整定义"。
```

**张力 T2：信息不足 vs 默认推进**

```text
因为信息不够就一直等 ←————————— 平衡点 ———————————→ 信息不够也瞎编默认值

平衡点：能默认的就用 mock/dry-run，不能默认的标为开放问题。
缺输入不等于不能推进——除非缺失项会导致当前闭环无法产生任何有意义产物。
```

#### 9 步推导的结构化路径

按以下顺序逐步推导，不要跳步。注意步骤之间的能量传递——**后面的步骤依赖前面的产出**。

```text
目标(1) + 成功标准(2) → 给"检查安排"(5)提供判断依据
成功标准(2) + 约束(3) → 给"持续推进策略"(6)提供边界
阻塞扫描(4) → 给"方案框架"(5)提供问题输入
方案框架(5) → 给"持续推进策略"(6)提供解决路径
检查安排(6) + 策略(7) → 给"可达性验证"(8)提供验证对象
```

**第 1 步：终态描述 → 目标**（方向线）

问自己：完成后世界长什么样？能否用一句话让不了解背景的人知道你要做什么？

```
❌ "优化一下代码" / "继续推进" / "让它更好" → 没有终态，会自动漂移
✅ "让 CLI 支持图片和文字一起进入 agent 上下文，并通过一次真实验证" → 有终态，可以判断
```

**第 2 步：什么证据能证明达到？ → 成功标准（分层）**（方向线 + 产物线）

成功标准必须分两层：
- **原型层**：系统是否工作？数据能通、过滤有结果、构建可通过。
- **效果层**：目标是否真正达成？用户行为会有什么可观察的变化？效果层在原型阶段无法直接验证时，必须设计替代信号——用已知正例/反例验证、用抽样验证替代全量。

判断标准：能否让一个不了解过程的人，仅凭证据判断"完成了"？

**第 3 步：什么方式达成我会觉得"不对"？ → 约束**（方向线 + 解释权线）

约束写的是"不能牺牲什么"，不是"应该怎么做"。分为：
- **硬约束**：违反后不可修复（数据丢失、安全泄露、法律风险）
- **软约束**：违反后可修复但有成本

**第 4 步：整个闭环里迟早需要什么资源/权限/决策？ → 启动前阻塞扫描**（解释权线）

不是列风险清单，而是提前拆除闭环断点。看整个目标闭环，不只看当前一圈。凡是迟早需要的资源、访问凭证、权限、外部服务选择、预算、数据边界、责任判断，都要提前列出。

每个阻塞项判断三件事：
1. 能否由人类现在提前解除？ → 能就前置解除
2. 不能解除，是否有 mock / dry-run / 占位接口 / 缩小目标等替代路径？ → 有就走替代
3. 不能解除也不可替代 → 才是当前阻塞

运行中才会暴露的问题（接口异常、测试失败、数据质量不足）只在执行后处理，不提前猜测。

**第 5 步：大致怎么补？ → 方案框架**（三线协同）

阻塞扫描告诉你"缺什么"，方案框架回答"大致怎么补"。它给出解决方向，不是实现细节。

问自己三个问题：

1. **整体解决方向是什么？**（方向，不是具体怎么做）
2. **方案由哪几个关键组件/机制组成？**（结构，不是文件/函数）
3. **什么不在方案范围内？**（边界，防止过度设计）

判断粒度的边界：

```text
方案框架应整理到：
  增加一个入口 Gate，只判断能否进入某个流程
  在自然停顿点触发 checkpoint，沉淀状态
  核心数据禁止直接写入，必须走正式通道

方案框架不应整理到：
  具体函数叫什么、放在哪个文件、参数怎么设计
  状态字段叫什么、if 条件怎么写、测试 fixture 怎么命名
```

**第 6 步：每做完一圈我怎么知道是对的还是偏的？ → 检查安排**（方向线 + 产物线）

检查安排不是最后验收，而是每一圈的校准机制。必须说明：
- 主 agent 自查什么产物和证据
- 独立检查 Agent 复查什么缺口和风险
- 什么检查结果会触发人类介入

人类不做默认检查者，只在检查结果触发人类介入点时出现。

**第 7 步：从当前状态到终态的路径是什么？ → 持续推进策略**（三线协同）

持续推进策略不能只写一个下一步，而是说明：**先做什么，做完怎么检查，没过怎么修，过了怎么继续，什么时候必须问人或完成**。

每一圈必须绑定三个锚点：
- **目标锚点**：这一圈离目标更近了吗？
- **产物锚点**：这一圈产出了什么可检查的东西？
- **解释权边界**：这个决定我有资格独自做吗？

**第 8 步：可达性验证**（方向线）

把所有圈串起来，逐一检查：每一圈关闭了最终目标的哪个缺口？所有成功标准都被至少一圈覆盖了吗？如果有标准没被覆盖，补圈或重设计。

**第 9 步：向用户提出三类问题**（解释权线）

输出目标循环后，用"请你确认 / 提供"收口。只问三类问题：
1. **校准判断**：会改变目标、成功标准、约束、责任边界或解释权的问题
2. **可提前解除的阻塞**：用户现在能提供、且能减少后续停顿的资源、凭证、权限、输入、偏好或关键决策
3. **默认推进策略**：如果用户暂时不确认或不提供，如何继续推进可逆、可检查的工作

不问的三类问题：
1. agent 可以按默认策略推进的问题
2. 只需记录、不影响当前推进的偏好
3. 可以用 mock / dry-run / 占位接口继续处理的细节

**对齐完成后，初始化 Loop State，进入 Phase 1。**

### Phase 1 — Execute Loop（循环执行，每圈重新微观规划）

```
1. 读取 Loop State（目标、成功标准、当前状态、已有证据）

2. 微观规划（模型的推理判断任务，Runner 不管这个）：
   - 上一圈的结果是什么？离目标更近了吗？
   - 策略仍然可达吗？需要调整吗？
   - 下一步应该做什么？
   - 根据判断更新 next_step

3. 执行 next_step

4. 写入 result / evidence / risks / open_questions / current_state

5. 运行 Session Runner Script

6. Runner 返回 continue → 回到步骤 1（重新做微观规划）

7. Runner 返回 human_gate / pause / complete → 交付状态，退出循环
```

关键说明：
- **微观规划是模型的判断能力，Runner 不做判断，只做结构检查。** Runner 是轨道，不判断走得好不好，保证不脱轨。
- Phase 0 只做一次（首次宏观规划）；Phase 1 每圈开头都要做微观规划。
- Runner 返回 continue 时，**不得询问用户"是否继续"**，直接进入下一圈。

## 强制执行协议

```text
1. 必须先完成 Phase 0（Plan & Align），输出完整的目标循环草案并与人类对齐。
2. 对齐确认后，初始化 Loop State（.goal-loop/loop_state.json），写入所有要素。
3. 运行 Session Runner Script（--phase plan）验证结构完整性。
4. 验证通过后，进入 Phase 1 循环。
5. 每圈先做微观规划，更新 state，再执行 next_step。
6. 每圈执行后写回 Loop State，运行 Runner。
7. Runner 返回 continue，立即执行新的 next_step。
8. Runner 返回 human_gate / pause / complete，按对应状态交付，不再继续。
```

不得用人工可读计划文档替代 Loop State。可以额外写 GOAL_LOOP.md，但它不能替代机器可读状态。

最小 Loop State 字段（完整 schema 见下方）：

```json
{
  "goal": "",
  "success_criteria": [],
  "constraints": [],
  "current_state": "",
  "evidence": [],
  "risks": [],
  "open_questions": [],
  "next_step": "",
  "human_gate": false,
  "pause": false,
  "complete": false,
  "completion_audit": null,
  "blocking_scan": {
    "pre_resolved": [],
    "future_blocking": [],
    "current_blocking": [],
    "missing_defaults": []
  },
  "check_arrangement": {
    "self_check": "",
    "independent_check": "",
    "human_gate_trigger": ""
  },
  "strategy": "",
  "feedback_rules": "",
  "human_gates": [],
  "pause_conditions": "",
  "completion_criteria": "",
  "alignment_questions": {
    "calibration": [],
    "pre_resolvable_blockings": [],
    "default_strategy": ""
  },
  "accessibility_verified": false
}
```

Runner 输出必须是以下四种之一：

```text
continue
human_gate
pause
complete
```

## 规则分层

以下规则分为两类，让模型清楚约束来源。

### 【Runner 兜底】有代码验证，违反就过不了 Runner

```text
- 必须有 .goal-loop/loop_state.json 文件，且所有必填字段存在、类型正确
- complete=true 时，completion_audit 必须非空且与 success_criteria 对应
- human_gate=true 或 human_gates 中存在 current_blocking 类型时，必须停下来交付
- continue 时不能询问用户"是否继续"，必须立即执行 next_step
- 不能在没有 completion_audit 时宣称 complete
```

Runner 兜底的规则由代码保障，不需要反复强调。

### 【模型自律】没有代码验证，靠 SKILL.md 的文本约束力

```text
- 成功标准必须分层（原型层 + 效果层），效果层必须写用户行为的可观察变化
- 约束要写出"不能牺牲什么"，避免表面完成但破坏真实目标
- 启动前阻塞扫描要覆盖整个闭环，不只看当前一圈
- 持续推进策略必须带三锚点（目标、产物、解释权边界）
- 检查安排要包含主 agent 自查和独立检查 Agent 复查
- 人类介入点要区分当前阻塞、未来阻塞、记录型
- 每圈结束做可达性重检：当前路径还能到达最终目标吗？
- 反馈可以改变下一步，但不能未经确认就改写目标
- 完成时必须做完成审计，防止把阶段节点误当成真正完成
```

## 判断边界

### 需要问人

```text
目标是否变化
成功标准是否变化
多个路线都合理但代价不同
用户体感是否接受
涉及钱、客户、法律、隐私或长期架构
真实凭证、付费资源、外部服务、外发数据或最终责任需要人确认
```

### 不需要问人

```text
修正明确错字
补一个缺失小节
执行已确认计划
整理当前状态
跑已确认的验证
做可逆的接口、mock、dry-run、占位实现或结构化草案
完成阶段收口后继续进入下一圈
```

### 人类介入点的思考方法

不是背清单，而是问两个问题：

```text
问题 A：这个决定如果错了，能撤回吗？（可逆性）
  → 能撤回 → agent 可以先做再看
  → 不能撤回 → 停下来问人

问题 B：做这个决定需要的判断，本质上属于谁？（解释权归属）
  → 纯技术判断（agent 有完整信息）→ agent 做
  → 涉及用户偏好/产品方向/业务取舍 → 问人
  → 用户表达了主观感受（"感觉不对"）→ 解释权在用户，必须问
```

组合判断：
```text
可逆 + 解释权在 agent → 直接做
可逆 + 解释权在用户 → 做了但标记，下次同步
不可逆 + 解释权在 agent → 做 + 通知
不可逆 + 解释权在用户 → 停下来问
```

人类介入点要区分阶段：
- **当前阻塞**：不回答就无法继续当前闭环，必须停下来问人
- **未来阻塞**：不影响当前原型、草案或可逆产物推进，但进入真实交付、上线、高风险使用前必须确认
- **记录型**：需要确认，但不阻止当前推进，只进入开放问题或待确认清单

## 反馈处理

反馈来了怎么处理？用四个问题：

```text
1. 这是什么类型的信号？
   错误 / 偏好 / 风险 / 机会 / 约束变化 / 目标变化

2. 谁有资格解释这个信号？
   agent 能自己判断？还是需要用户/专家/测试来确认？

3. 这个信号应该更新什么？
   目标 / 计划 / 标准 / 上下文 / 风险记录 / 仅观察

4. 这个信号对循环的影响是什么？
   继续 / 重试 / 改计划 / 收紧目标 / 扩大目标 / 暂停 / 完成
```

信号过滤的判断方法：每个信号问——**它改变了我对"离目标多远"的估计吗？**

```text
改变了距离估计 → 立即响应（可能需要改路径）
改变了路径偏好 → 记录，在下一个决策点考虑
不改变任何估计 → 记录但不行动
与目标无关 → 丢弃
```

## 上下文管理

如果下一轮的 agent 是一个全新的人，他需要知道什么才能接手？那些东西就是必须跨轮存活的上下文。

三层保留策略：

```text
永久层（方向）→ 永不压缩：
  Goal / Constraints / Success Criteria + 关键决策的 why

摘要层（位置）→ 保留摘要：
  Current State / Failed Attempts / Open Questions

临时层（过程）→ 可丢弃：
  执行细节、中间草稿、已验证通过的步骤
```

## 输出格式

### 初始化时（Phase 0 输出）

```markdown
## 目标循环

**目标:** [完成后的世界长什么样]

**成功标准:** [什么算完成，尽量可观察。分层：原型层（功能可用，构建时可验证）+ 效果层（目标真正达成，需要真实使用后才能验证）。效果层必须写用户行为的可观察变化，不是系统自证。效果层在原型阶段无法直接验证时，设计替代信号。]

**约束:** [不能用什么代价达成，哪些边界不能越过]

**当前状态:** [现在已经有什么，还缺什么]

**启动前阻塞扫描:** [整个闭环有哪些必经资源、访问凭证、权限、外部服务选择、预算、数据边界、责任判断和关键决策；哪些能由人类提前解除；哪些不能提前解除但可用 mock、dry-run、占位接口或缩小目标替代；哪些不能解除也不可替代，必须当前阻塞]

**方案框架:** [整体解决方向，方案由哪些关键组件/机制组成，什么不在范围内。整理到策略级方向，不到实现细节。]

**检查安排:** [每一圈/每一步推进后，主 agent 检查什么产物和证据；独立检查 Agent 复查什么缺口、风险和偏差；检查结果如何触发人类介入点]

**持续推进策略:** [从当前状态开始，agent 如何按"执行 -> 检查 -> 修正 / 继续 / 问人 / 完成"循环推进；先做什么，之后如何继续。写完后做可达性验证：把所有圈串起来，逐一标记每一圈关闭了最终目标的哪个缺口；如果有成功标准没被任何圈覆盖，补圈。]

**反馈规则:** [新反馈来了怎么处理]

**人类介入点:** [什么时候必须问人，区分当前阻塞 / 未来阻塞 / 记录型]

**暂停条件:** [什么时候不能继续；暂停时要交代当前状态、已完成证据、卡点和下一轮接续点]

**完成条件:** [什么证据证明已经满足成功标准；完成时必须做完成审计]

## 请你确认 / 提供

**需要你校准的判断:** [只列会改变目标、成功标准、约束、责任边界或解释权的问题；没有就写"暂无"]

**可提前解除的阻塞:** [用户现在能提供、且能减少后续停顿的资源、凭证、权限、输入、偏好或关键决策；没有就写"暂无"]

**默认推进策略:** [如果用户暂时不确认或不提供，agent 如何继续推进可逆、可检查的工作]
```

### 触发 Human Gate 时输出

```markdown
## 需要人类判断

**当前状态:** [我在哪]
**遇到的情况:** [为什么停下来]
**可选路径及代价:**
1. [路径 A] — [代价]
2. [路径 B] — [代价]
**推荐:** [我觉得哪条好，为什么]
```

### 完成审计时输出

```markdown
## 完成审计

**原目标:** ...
**成功标准:** ...
**交付物与证据:** ...
**遗留风险:** ...
**结论:** [我如何知道它完成了]
```

## 示例

示例只说明原则，不代表只能用于这些场景。圈数不是固定格式——任务小可以两圈，任务大可以三圈或更多，关键是每一圈都绑定可检查产物。

### 示例 1：调研任务

用户说：

```text
帮我把这个调研任务推进完。
```

**Phase 0 推导过程：**

1. 终态：用户说"推进完"，但没说清是什么调研。先问清楚→ 假设是"调研竞品 AI 助手的功能差异，形成决策建议"。
2. 成功标准：原型层——至少比较 3 个竞品，有证据和推荐；效果层——决策者 5 分钟内能说清倾向哪个方案。
3. 约束：可靠来源，不写未验证的结论，不做成长报告。
4. 阻塞扫描：需要用户确认调研范围、目标竞品、决策场景。这些都能现在确认。
5. 方案框架：先横向对比信息源，再逐层深入；用标准化表格承载每个竞品的比较，用独立结论页收口推荐。
6. 检查安排：每圈自查产物是否有证据、风险和待确认项。
7. 策略：第一圈出候选方案表 → 第二圈出比较结论和推荐 → 第三圈出最终简明结论。
8. 可达性验证：三圈分别覆盖"信息源从哪来"、"哪个更好"、"结论是什么"，覆盖所有成功标准。

**输出：**

```markdown
## 目标循环

**目标:** 完成竞品 AI 助手功能差异调研，形成一份可以用于决策的简明结论，说明可选方案、主要证据、风险和推荐路径。

**成功标准:**
- （原型层）至少比较 3 个竞品，每个有核心依据和主要风险
- （原型层）输出一个明确推荐，标出哪些结论确定、哪些需要人确认
- （效果层）决策者能在 5 分钟内说清倾向哪个方案及其理由

**约束:**
- 优先使用可靠来源，不把未经验证的信息写成确定结论
- 不展开成过长报告，先给可决策版本

**当前状态:** 已有一个初步问题，但还没有系统比较方案，也没有形成推荐。

**启动前阻塞扫描:**
- 已知必需输入：调研问题、目标竞品、决策场景
- 可提前解除：用户可在启动前给出调研范围和风险偏好
- 可预判未来阻塞：如果推荐要进入真实客户交付或公开发布，需要确认责任边界和最终确认人
- 运行中暴露问题：资料质量不足、关键来源互相冲突
- 缺失默认策略：先用标注不确定性的草案继续推进

**方案框架:**
- 解决方向：先横向收集竞品，再逐层深入对比，最后收口推荐
- 关键组件：标准化竞品对比表 → 比较结论 → 推荐结论页
- 不在范围内：不展开每个竞品的全面分析，只提取与决策相关的维度

**检查安排:**
- 主 agent 自查：每一圈检查是否产出候选方案表、比较结论或最终简明结论；产物是否有证据、风险和待确认项
- 独立检查 Agent 复查：是否存在证据不足、遗漏关键竞品、把猜测写成结论等风险
- 触发人类介入：推荐涉及风险偏好或高代价取舍时

**持续推进策略:**
- 第一圈：产出候选方案表（至少 3 个竞品，证据 + 风险）→ 关闭"有哪些选项"缺口
- 第二圈：产出比较结论和推荐路径 → 关闭"哪个更好"缺口
- 第三圈：产出最终简明结论、风险清单和待确认问题 → 关闭"结论是什么"缺口
- 可达性验证：三圈覆盖所有成功标准

**反馈规则:** 如果新资料只补充细节，记录即可；如果推翻关键假设，更新推荐路径；如果资料来源不可靠，只作为线索，不进入结论。

**人类介入点:**
- 当前阻塞：必须在多个高代价推荐路径中选择一个，且选择会影响当前交付责任或明显资源投入
- 未来阻塞：推荐结论要进入真实客户交付、公开发布或长期战略承诺前，需要人确认
- 记录型：需要确认决策偏好或风险承受度，但不影响继续整理证据

**暂停条件:** 如果关键证据缺失且无法继续验证，暂停并说明缺口、已完成证据和下一轮应补的资料或判断。

**完成条件:** 当推荐路径、证据、风险和待确认问题都写清楚，并完成一次审计后，本轮完成。

## 请你确认 / 提供

**需要你校准的判断:** 这次调研是否会用于真实决策、客户交付或公开发布；如果会，推荐路径的风险偏好需要你确认。

**可提前解除的阻塞:** 如果已有指定资料源、必须比较的方案、内部约束或不能采用的路线，可以现在提供。

**默认推进策略:** 如果暂时不提供，先用公开资料和标注不确定性的候选方案表推进。
```

**Runner 交互标注（Phase 1 示例）：**

```text
[Runner --phase plan] → 验证 Loop State 结构完整，通过
[第 1 圈] 执行：调研竞品 A/B/C 功能 → 写入 evidence → Runner: continue
[第 2 圈微观规划] 已有 3 个竞品数据，策略可达，next_step 更新为"比较分析"
[第 2 圈] 执行：比较分析 → 写入 result → Runner: continue
[第 3 圈微观规划] 比较完成，策略可达，next_step 更新为"写最终结论"
[第 3 圈] 执行：写结论 → 写入 result → 做 completion_audit → Runner: complete
[交付] 输出完成审计报告，退出循环
```

### 示例 2：产品改进任务

用户说：

```text
帮我把这个功能体验优化一下。
```

**Phase 0 推导过程：**

1. 终态：找出最影响体验的 1-2 个问题，完成一轮低风险改进。
2. 成功标准：原型层——能说出问题、完成一处可验证小改动；效果层——用户完成核心动作的时间或步骤数减少。
3. 约束：不大范围重构，不引入新复杂依赖，不用主观喜好替代用户体感。
4. 阻塞扫描：需要确认功能目标、核心使用路径、允许改动范围。都能现在确认。
5. 方案框架：先复现核心使用路径找卡点，再做低风险小改验证，不重构原有流程。
6. 检查安排：每圈自查是否产出卡点列表、小改、验证结果。
7. 策略：第一圈复现路径找卡点 → 第二圈做小改验证 → 处理下一个或完成。
8. 可达性验证：两圈覆盖"问题在哪"和"改了有没有效"，效果层标准需要真实用户验证，设计替代信号——用已知任务路径的完成时间和步骤数变化作为替代。

**输出：**

```markdown
## 目标循环

**目标:** 找出这个功能当前最影响用户体验的 1-2 个问题，并完成一轮低风险改进，使用户能更顺畅地完成核心动作。

**成功标准:**
- （原型层）能说清当前体验问题是什么；至少完成一处可验证的小改动；能通过一次基本使用验证
- （原型层）改进方案不改变功能主目标
- （效果层）用户完成核心动作的时间或步骤数减少，或主观反馈中不再提到该卡点

**约束:**
- 不做大范围重构，不引入新的复杂依赖
- 不用主观喜好替代用户体感

**当前状态:** 已知道用户觉得体验不顺，但还没有定位具体卡点。

**启动前阻塞扫描:**
- 已知必需输入：功能目标、当前核心使用路径、这轮允许改动的范围
- 可提前解除：用户可确认功能目标、核心使用路径、允许改动范围和不可碰的边界
- 可预判未来阻塞：如果要改变核心流程或影响真实用户，需要确认发布权限、回滚方式和最终责任人
- 运行中暴露问题：实际卡点和预期不一致、改动引入新摩擦
- 缺失默认策略：先做低风险、可回退的小改或原型记录

**方案框架:**
- 解决方向：先走通路径找卡点，再做低风险可回退的小改，不碰核心流程
- 关键组件：使用路径记录 → 卡点列表 → 小改 + 验证 → 遗留风险记录
- 不在范围内：不改变功能主目标，不做架构级优化

**检查安排:**
- 主 agent 自查：每一圈检查是否产出使用路径记录、卡点列表、小改、验证结果或遗留风险；产物是否仍在目标和约束内
- 独立检查 Agent 复查：是否漏掉更明显的用户卡点；是否把主观喜好当成体验问题
- 触发人类介入：涉及产品方向变化、用户群取舍或核心流程改变时

**持续推进策略:**
- 第一圈：产出使用路径记录和最明显卡点列表 → 关闭"问题在哪"缺口
- 第二圈：产出一处小改、基本验证结果和遗留风险 → 关闭"改了有没有效"缺口
- 如果验证未通过但原因明确，继续修；如果通过且未触发人类介入，继续处理下一个卡点或进入完成审计
- 可达性验证：两圈覆盖所有原型层标准，效果层用替代信号验证

**反馈规则:** 如果问题是明确交互摩擦，先做小改；如果涉及产品方向或目标用户变化，停下来请人判断。

**人类介入点:**
- 当前阻塞：是否改变原有核心流程，且改动不可轻易回退；是否牺牲某类用户体验换另一类且会影响产品方向
- 未来阻塞：该体验改动进入正式发布、影响真实用户或改变长期产品定位前，需要人确认
- 记录型：是否需要重新定义功能目标；当前小改不依赖这个判断时先完成本轮验证

**暂停条件:** 如果发现问题来自更上层产品定位，当前低风险改动无法继续判断，暂停并说明当前卡点、已验证改动、遗留风险和下一轮应继续判断的位置。

**完成条件:** 当一个明确卡点被修正，并通过基本使用验证，且遗留风险已记录后，本轮完成。

## 请你确认 / 提供

**需要你校准的判断:** 是否存在不可改的核心流程、不能牺牲的用户群体体验，或这轮不能触碰的产品边界。

**可提前解除的阻塞:** 如果有用户反馈、录屏、日志、失败路径或发布限制，可以现在提供。

**默认推进策略:** 如果暂时不提供，先基于现有路径做低风险复现和可回退小改。
```

**Runner 交互标注（Phase 1 示例）：**

```text
[Runner --phase plan] → 验证 Loop State 结构完整，通过
[第 1 圈] 执行：复现核心使用路径 → 写入卡点列表 → Runner: continue
[第 2 圈微观规划] 卡点明确，策略可达，next_step 更新为"做低风险小改"
[第 2 圈] 执行：小改 + 基本验证 → 写入 result + evidence → Runner: continue
[第 3 圈微观规划] 验证通过，策略可达，检查还有没有其他卡点
[第 3 圈] 执行：处理第二个卡点或进入完成审计 → Runner: complete
[交付] 输出完成审计报告，退出循环
```
