# Agent 多轮任务为什么会跑偏，以及我们能做什么

## 一个现象

给 AI 一个模糊任务（"帮我做一个调研""把这个功能优化一下""继续推进"），它会开始干活。但很快出现三种情况：

- **漂移**：做着做着方向变了，最初要解决的问题被忘了
- **停产**：一直在"分析""定义""设计"，始终不产出可检查的东西
- **越界**：在应该问人的时候自己做了判断——"我觉得用户应该会喜欢这样"

这组问题不是某个模型的缺陷，是**多轮任务的固有属性**：需要多步推进、过程中会出现新信息、某些判断超越了模型的信息和权限边界。

## 问题出在哪里

我让不同模型跑相同的多轮任务，反复观察后发现了三个根因。

### 根因 1：没有锚点

```
用户给的是一句模糊指令
→ 模型第一次执行时，"目标"是在上下文里的
→ 第三轮、第四轮，上下文变长、新信息涌入
→ 目标被稀释，模型开始回应自己上一轮的产出而不是最初要解决的问题
```

本质是：模糊任务 + 多轮上下文膨胀 = 目标自动漂移。这不是"模型不够好"，是被迫在持续变化的信息场里保持方向。

### 根因 2：没有产物模型

模型有一个倾向：把"理解问题"等同于"解决问题"。所以它会——

```
第一轮：分析需求、定义边界、梳理选项
第二轮：细化分析、补充更多背景
第三轮：还在分析
→ 用户说"推完了吗" → 模型说"我整理一下现状" → 继续分析
```

本质是：在没有显式产物要求的情况下，模型默认"思考 = 工作"，"定义清楚 = 做完了"。它不是懒惰，它是一个被训练成"先全面理解再行动"的系统——在多轮场景里，这个倾向会扼杀产出。

### 根因 3：没有解释权边界

模型做判断时，它不知道自己做判断的成本。它有自己的"自信"——

```
模型觉得"这个设计改动应该行"
→ 但改动背后是一个用户群的体验取舍
→ 模型没有这些用户的信息
→ 它也没问——因为它不觉得"缺信息"
```

本质是：模型的判断力和它做判断的信息权限之间，没有任何机制在做区分。它在所有问题上都有能力生成答案，但不是所有问题它都有资格回答。

## 一种解法：显式 Plan 阶段

针对这三个根因，我设计了一个在进入执行循环**之前**插入的显式规划阶段。它的核心思想很简单：**任务来了，先别急着干。先把"要做什么""什么算完成""什么不能碰"对齐清楚。**

### 这个提法为什么有效

在多轮执行之前显式插入一个规划和对齐阶段，改变了几件事：

**对"没有锚点"**：Plan 阶段产出一个被明确定义的终态描述和成功标准。后续执行中，目标不再依赖模型的短期记忆——它是被写下来、被结构化记录的状态，不会被上下文膨胀稀释。

**对"没有产物模型"**：Plan 阶段要求持续推进策略里的每一圈都绑定一个可检查的产物（文件、原型、验证结果——不是"分析一下""研究一下"这类动作描述）。这样模型在每一圈都有一个产出信号，不会被自己"还在思考"的惯性拖住。

**对"没有解释权边界"**：Plan 阶段要求提前做启动前阻塞扫描——把整个闭环里迟早需要的外部资源、访问凭证、权限、预算、数据边界和责任判断都列出来，区分哪些能由人类提前解除、哪些能在执行中用 mock 或 dry-run 替代、哪些必须停下来问人。这样人类介入点从一开始就进入协议，不是出了问题再补救。

### 结构：8 步推导路径

Plan 阶段的推导需要沿着一个思考路径来走，每步都有自己的产出，后面依赖前面。具体如下：

**1. 终态描述 → 目标**

问自己：完成后世界长什么样？能否用一句话让不了解背景的人知道你要做什么？

```
❌ "优化一下代" / "继续推进" / "让它更好"
   没有终态，第三圈就会忘了方向
✅ "让 CLI 支持图片和文字一起进入 agent 上下文，并通过一次真实验证"
   有终态，可以判断是否到达
```

**2. 什么证据能证明到了？ → 成功标准（分层）**

成功标准必须分两层：
- **原型层**：系统是否工作？数据能通、过滤有结果、验证可通过。
- **效果层**：目标是否真正达成？用户行为会有什么可观察的变化？如果效果层在执行时无法直接验证（比如还没有真实用户），必须设计替代信号——用已知正例/反例、用抽样验证、用模拟数据。

判断标准：能否让一个不了解过程的人，仅凭证据判断"完成了"？

**3. 什么方式达成我会觉得"不对"？ → 约束**

约束写的是"不能牺牲什么"，不是"应该怎么做"。分为：
- **硬约束**：违反后不可修复（数据丢失、安全泄露、法律风险）
- **软约束**：违反后可修复但有成本

约束的思考方法：问自己"如果模型用了某种方式达成了目标，我会不会觉得不对？"那个"不对"就是约束。

**4. 整个闭环里迟早需要什么资源/权限/决策？ → 启动前阻塞扫描**

看整个目标闭环，不只看当前一圈。凡是迟早需要的资源、凭证、权限、外部服务选择、预算、数据边界和责任判断，都要提前列出。每个阻塞项判断三件事：

1. 能否由人类现在提前解除？ → 能就前置解除
2. 不能解除，是否有 mock / dry-run / 占位接口 / 缩小目标等替代路径？ → 有就走替代
3. 不能解除也不可替代 → 才是当前阻塞

运行中才会暴露的问题（接口异常、测试失败、数据质量不足）只在执行后处理，不提前猜测。

**5. 每做完一圈我怎么知道是对的还是偏的？ → 检查安排**

检查安排不是最后验收，而是每一圈的校准机制。必须说明：
- 主 agent 自查什么产物和证据
- 独立检查 agent 复查什么缺口和风险
- 什么检查结果会触发人类介入

人类不做默认检查者，只在检查结果触发人类介入点时出现。

**6. 从当前状态到终态的路径是什么？ → 持续推进策略**

持续推进策略不能只写一个"下一步"，而是说明：先做什么，做完怎么检查，没过怎么修，过了怎么继续，什么时候必须问人或完成。

每一圈必须带着三个判断：
- 目标判断：这一圈离目标更近了吗？
- 产物判断：这一圈产出了什么可检查的东西？
- 权限判断：这个决定我有资格独自做吗？

**7. 可达性验证**

把所有圈串起来，逐一检查：每一圈关闭了最终目标的哪个缺口？所有成功标准都被至少一圈覆盖了吗？如果有标准没被覆盖，补圈或重设计。

这是一步自觉检查——防止局部每一圈都对、但整个路径不通。

**8. 向用户提问**

Plan 阶段走完，向用户收口。只问三类问题：
- 会改变目标、成功标准或约束的判断
- 用户现在能提供、且能减少后续停顿的资源或决策
- 如果用户暂时不确认，默认推进策略是什么

不问的：可以由 agent 默认推进的、可以用 mock 替代的、不影响当前闭环的偏好。

### 这 8 步本身遵循的原则

做 8 步不是为了把一个简单任务复杂化，而是因为你看到的模糊（"帮我做好"）到了你要开始做的时刻，必须变成一次明确的边界的确认。8 步走完，你对"要干什么""干到什么程度""不乱干什么"有了一个答案，后续执行才不会迷路。

核心原则可以概括为三条：

- **方向不漂移**：终态描述和成功标准必须让不熟悉背景的人也能判断是否到达
- **产物不断供**：每一圈必须有可检查的东西，不能停在"定义清楚"
- **解释权不越界**：agent 不代替人类做价值判断，只在有完整信息的技术问题上自主

## 已经验证了什么

我按这个结构推进过不同类型的多轮任务——调研在这个链路下会先在竞品候选、证据分布和推荐边界上有一个整的判断，产品改进则不跳到"改什么"的猜测，而是在走通核心使用路径后得到一个有具体锚点的卡点列表和可回退的小改方向。

这几个案例说明：Plan 阶段的显式化不是理论推演，是际能改变模型在模糊任务中跳步倾向的一种方法。

## 一个局限

这里我没有讲怎么让 agent **自动**推进每一圈。也就是说：

- Plan 阶段给出了完整的目标、约束和路径 —— 这个文写了
- 但要能不能在每一圈里自动判断"该继续/该问人/该停"，以及在状态信息上不丢失 —— 这个文没写

（这部分思路正在一个工具里验证。）

## 这个东西的用法

这不是一个跑在后台的"系统"。它是一套你可以嵌入提示语（Skill / System Prompt / Instruction）里的规划方法。用法是：

1. 把 8 步路径写进你的提示语，作为触发模糊任务时必须先走的阶段
2. 确保模型在进入执行之前，先把这 8 步的输出填完（不是心里想一遍，是输出出来）
3. 对齐完成后再往下走。不到对齐，不操作

你可以在你目前用的任何一个支持自定义提示语的 agent 里试。
